"""package containing directories for a feature based organization of the codebase.

Each feature subdirectory contains:
+ service: one or multiple services file that contins business logic (BL).
+ routes: one or multiple files that contains the routes.
"""
